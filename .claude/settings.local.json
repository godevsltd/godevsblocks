{
  "permissions": {
    "allow": [
      "Bash(php -d memory_limit=1G vendor/bin/phpstan analyse --no-progress)",
      "Bash(php vendor/bin/phpcs --standard=phpcs.xml)",
      "Skill(artifact-design)",
      "Skill(artifact-design:*)"
    ]
  }
}
